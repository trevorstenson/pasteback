# Swift Package Manager build artifacts
.build/
.swiftpm/

# Assembled app bundle (produced by scripts/build.sh)
PasteBack.app/
*.app/

# macOS
.DS_Store

# Local diagnostic log (scripts/Logger writes to /tmp, but just in case)
pasteback.log

# Local-only working-state notes (not for the public repo)
STATE.md
